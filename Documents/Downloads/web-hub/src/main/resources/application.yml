spring:
  application:
    name: web-hub

  profiles:
    active: dev

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/web_hub}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:smartcode}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:stanleyshadrack213@gmail.com}
    password: ${MAIL_PASSWORD:hozpltwxbixcenpn}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true


jwt:
  secret: ${JWT_SECRET:JK1pNC7VMEhjqqK9CMypsw0ybDLJtzDDLk3N266Ld/k=}

  expiration: 86400000

app:
  admin:
    email: ${APP_ADMIN_EMAIL:shazanaombonya@gmail.com}
    password: ${APP_ADMIN_PASSWORD:shazana123}
